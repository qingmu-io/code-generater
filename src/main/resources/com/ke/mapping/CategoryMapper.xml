<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ke.mapper.CategoryMapper" >

<resultMap id="baseResultMap" type="com.ke.entity.Category" >
  		<id column="id" property="id" />
 	  	 <result column="parent_id" property="parentId" />
 	  	 <result column="name" property="name" />
</resultMap>
  
  
  
<insert id="insert" parameterType="com.ke.entity.Category" useGeneratedKeys="true" keyProperty="id" >
  insert into `category` (parent_id,name)
  values (#{parentId},#{name})
</insert>
  
<delete id="delete">
    delete from `category` where id = #{id}
</delete>
  
  
<update id="update" parameterType="com.ke.entity.Category" >
    update `category`
    <set >
    	  id = #{id},
    	  parent_id = #{parentId},
    	  	name = #{name}
    </set>
    where id = #{id}
 </update>
  
  
<update id="merge" parameterType="com.ke.entity.Category" >
    update `category`
<set>
 	 <if test="id != null" >
    	  id = #{id},
     </if>
 	 <if test="parentId != null" >
    	  parent_id = #{parentId},
     </if>
     <if test="name != null" >
     	  name = #{name}
     </if>
    </set>
    where id = #{id}
  </update>
 
<select id="findOne" resultMap="baseResultMap" parameterType="long">
	select  
		id,parent_id,name
	from
		`category`
	where id = #{id}	
</select>
  
    
</mapper>